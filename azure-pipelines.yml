jobs:
  - job: BuildAndPublish
    displayName: "Build and Publish Blazor WebAssembly Project"
    steps:
      - task: UseDotNet@2
        inputs:
          packageType: 'sdk'
          version: '8.x'  # .NET-Version; passe hier an, falls nötig
          installationPath: $(Agent.ToolsDirectory)/dotnet
